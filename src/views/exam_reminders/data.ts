export interface examContent {
    action: string;
    examContentCode: string;
    offcialAccountCode: string | number
}

export interface enabledExamRemind {
    action: string;
    examId: string | number;
}
